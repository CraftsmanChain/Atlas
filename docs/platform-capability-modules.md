# ATLAS 平台能力模块与版本基线

> 产品名称：ATLAS — Infrastructure Hardware Reliability Workbench  
> 平台版本：v0.8.0
> 能力基线日期：2026-07-20  
> 文档更新日期：2026-07-24

## 1. 正式产品介绍

ATLAS 是面向 GPU 集群并可扩展至服务器、存储和网络基础设施的硬件可靠性工作台，提供资产对账、监控数据质量发现、硬件健康评分、故障检测、硬件故障预警与预测、性能衰减识别、故障事件管理以及维修验证闭环。

当前实现以 GPU 为第一硬件对象。服务器、存储和网络硬件沿用相同的资产、数据质量、健康、事件、预测和维修验证模块扩展，不另建割裂的平台。

## 2. 模块版本规则

平台能力按模块独立采用语义版本 `MAJOR.MINOR.PATCH`：

- `MAJOR`：模块 API、状态语义、评分定义或数据契约发生不兼容变化
- `MINOR`：新增向后兼容的指标、规则、硬件类型、页面或工作流能力
- `PATCH`：规则阈值校准、缺陷修复、性能优化和展示修正
- `v0.0.0`：已完成路线设计，但尚未交付可运行模块
- `v0.x`：模块处于增量开发阶段，接口或状态语义仍可能调整
- `v1.x`：已经形成可运行、可验收和有版本契约的稳定基线

每次模块发布必须同步更新：模块版本、状态、功能范围、变更记录、数据/API 契约和验证结果。

平台页面默认仅展示各模块最近 3 次迭代，完整历史通过模块详情弹窗查看；本文件保留全部迭代记录，避免页面随版本增长失控。

## 3. 当前能力模块

| 模块 ID | 模块名称 | 功能范围 | 当前状态 | 当前版本 | 迭代历史 |
| --- | --- | --- | --- | --- | --- |
| `asset` | 资产对账 | 以 LXOPEasier 为资产事实源；GPU 身份与四类 Target 每 10 分钟以单飞任务同步 | 监控侧基线 | `v0.2.1` | `v0.2.0` 明确事实源；`v0.2.1` GPU 身份与 Target 合并为 10 分钟非重入对账，恢复状态同批更新 |
| `quality` | 监控数据质量发现 | 每卡样本存在率、年龄、最大间隔、UUID 波动和 DCGM Target 抓取成功率、样本量、耗时变化 | 链路连续性基线 | `v0.6.0` | `v0.5.0` 每卡连续性闭环；`v0.6.0` gap、flap 与 Target 抓取质量 |
| `health` | 硬件健康评分 | DCGM 主源、gpu_exporter 补充与降级备用；快照携带 GPU 时序及采集链路结构特征，结构异常不判为硬件故障 | 结构质量增强 | `v1.4.0` | `v1.3.0` 样本存在率和年龄；`v1.4.0` gap、flap 与 Target 抓取质量 |
| `detection` | 故障检测 | 识别已发生或正在发生的 ECC、XID、掉卡、不可用、温度和 PCIe 等故障；AI + Skill 辅助生成故障报告、根因分析和处理建议 | 规则基线；AI 辅助路径已定义、未开发 | `v0.1.0` | `v0.1.0` Row Remap、温度、PCIe 和 XID 变化确定性规则；AI + Skill 实现路径定义 |
| `feature` | 统一特征目录 | 为健康评分、PyOD、风险排序、监督预测和性能衰减提供统一版本化、双源、趋势与结构质量契约 | 结构契约增强 | `v1.4.0` | `v1.3.0` 30 个特征、48 个查询；`v1.4.0` 35 个特征、53 个查询及采集链路结构质量 |
| `prediction` | 硬件故障预警与预测 | 故障发生前的风险预警与概率预测，覆盖 GPU，并扩展至服务器、存储和网络硬件 | 特征底座就绪；模型未交付 | `v0.0.1` | `v0.0.0` 模型路线；`v0.0.1` Feature Catalog v1 可供异常检测、风险排序和监督训练消费 |
| `degradation` | 性能衰减识别 | 基于 Feature Catalog 快照的高负载 SM 时钟被动同类比较；后续扩展显存、PCIe、NVLink 和维护窗口主动验证 | 被动影子基线 | `v0.1.0` | `v0.0.0` 被动/主动路径；`v0.0.1` 特征与验证契约；`v0.1.0` 同节点同型号优先、同型号集群兜底的中位数基线、候选 API 与页面 |
| `incident` | 告警中心 | 接收记录、规则事件去重、episode 生命周期、硬件告警详情和人工处置 | 硬件处置基线；案例开发中 | `v0.3.0` | `v0.2.2` 硬件事件稳定分页；`v0.3.0` 硬件告警关联处置台账，支持详情、根因、方案、过程、结果和训练资格录入 |
| `issue` | 数据统计 | 当前资产范围内的数据质量、资产身份与节点可用性统计、状态钻取和人工处置；硬件故障归入告警中心 | 数据问题闭环基线 | `v0.3.0` | `v0.2.3` 恢复状态同批更新；`v0.3.0` 更名为数据统计，默认口径排除硬件故障并保留底层训练台账 |
| `platform` | 平台实例配置 | 实例名称、产品名称、副标题与环境标识的运行时读取、页面维护和统一展示 | 配置基线完成 | `v0.1.0` | `v0.1.0` 单例持久化 API、平台概览配置页、即时预览与全局展示联动 |
| `validation` | 维修验证闭环 | 维修反馈、根因标签、修复/更换结果和重新上线复测 | 方案阶段 | `v0.0.0` | 状态机、维护窗口和验证门设计完成 |

## 4. 平台开发里程碑

| 阶段 | 范围 | 状态 |
| --- | --- | --- |
| P0 | 资产、Target、身份、同步、数据正确性和平台实例配置底座 | 完成（平台实例配置 v0.1.0） |
| P1 | GPU 健康评分、双源补充/回退、来源审计与数据置信度 | 基线完成（健康评分 v1.4.0） |
| P2 | 告警中心、数据问题台账、人工处置反馈、案例和维修闭环 | 开发中（告警中心与数据统计 v0.3.0 完成，案例审批与复测待开发） |
| P2.5 | 性能衰减检测与维护窗口验证 | 开发中（被动 SM 时钟影子基线完成，主动验证待开发） |
| P3 | 统一特征底座、无监督异常检测与群体/历史基线 | 开发中（Feature Catalog v1.4 结构契约增强，metric-family recording rule、历史基线与 PyOD 未交付） |
| P4 | GPU 及服务器、存储、网络硬件的故障预警与监督式预测 | 规划 |
| P5 | LLM 解释、RCA、SOP 和知识沉淀 | 规划 |

事件链路与特征库的当前诊断、详细数据契约、参考项目适配结论、交付版本和汇报指标见 [事件链路与故障特征库实施方案](event-pipeline-and-fault-feature-catalog-plan.md)。

## 5. 模块架构与关系

![ATLAS 平台模块架构](../web/public/atlas-platform-architecture.svg)

架构中的资产主数据以 [LXOPEasier](https://10.111.201.1:7090/) 为准；Prometheus/DCGM、系统日志和 BMC/IPMI 构成运行观测与故障详情。健康评分表达当前状态，故障检测确认已经发生或正在发生的问题，硬件故障预警与预测评估故障发生前风险，事件管理、AI + Skill 辅助和维修验证组成处置闭环。AI + Skill 当前为已确定实现路径，未进入开发。

## 6. 版本更新要求

模块发生功能变更时，应在同一次迭代中完成：

1. 更新本文件中的模块版本、状态和迭代历史。
2. 更新平台“能力与里程碑”页面。
3. 更新相关 API、数据模型、规则或算法版本。
4. 记录测试环境验证结果和已知限制。
5. 稳定运行并完成验收前，不将规划能力描述为已经交付。
